{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "buildCommand": "npm install && npx tsc && npx prisma generate",
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
